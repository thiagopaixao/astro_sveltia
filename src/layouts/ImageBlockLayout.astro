---
const { Component, props } = Astro.props;
const { src, alt, wide } = props;

const componentData = {
    src,
    alt: alt || "",
    wide: wide || false
};
---

<Component {...componentData}>
    <slot />
</Component>
