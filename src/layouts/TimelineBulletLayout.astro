---
const { Component, props } = Astro.props;
const { text, contentBullet } = props;

console.log("Conteúdo TimelineBullet: ", contentBullet )
---
<Component text={text} contentBullet={contentBullet} >
    <div set:html={contentBullet} >                                                   
        <slot />                                                                      
    </div>

</Component>
