---
const { Component, props } = Astro.props;
const { chartTitle, chartNotes, chartBars } = props;

const componentData = {
    title: chartTitle,
    notes: chartNotes,
    values: chartBars
};
---

<Component {...componentData}>
    <slot />
</Component>
