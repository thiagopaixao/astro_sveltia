---
const { Component, props } = Astro.props;
const { txtColor, bgColor, content, ...otherProps } = props;
---

<Component txtColor={txtColor} bgColor={bgColor} content={content} {...otherProps}>
  {content && <p>{content}</p>}
  <slot />
</Component>
