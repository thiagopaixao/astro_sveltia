collections:
  - name: 'pages'
    label: 'Páginas'
    folder: 'src/content/pages'
    create: true
    slug: '{{slug}}'
    fields:
      - { label: 'Título', name: 'title', widget: 'string', required: false }
      - label: 'Sessão'
        name: 'session'
        widget: 'list'
        collapsed: true
        required: false
        types:
          - *session

